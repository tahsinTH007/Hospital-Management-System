{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "react-router",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "${BACKEND_URL}/api/$1",
      "env": ["BACKEND_URL"]
    }
  ]
}
